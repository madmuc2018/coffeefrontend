<?xml version="1.0" encoding="UTF-8"?>
<ContentPage 
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:coffeefrontend;assembly=coffeefrontend"
    x:Class="coffeefrontend.UpdateOrderPage">
    <ContentPage.Content>
        <StackLayout Padding="20">
            <Label Text="Update Order" FontSize="Large" HorizontalOptions="CenterAndExpand" VerticalOptions="CenterAndExpand"/>

            <Label Text="ID" FontSize="Small"/>
            <Entry Text="{Binding SelectedOrder.id, Mode=TwoWay}" FontSize="Small"/>
            
            <Label Text="Type"/>
            <Entry Text="{Binding SelectedOrder.type, Mode=TwoWay}" FontSize="Small"/>
            
            <Label Text="Quantity" FontSize="Default"/>
            <Entry Text="{Binding SelectedOrder.quantity, Mode=TwoWay}" FontSize="Small"/>
            
            <Label Text="From" FontSize="Default"/>
            <Entry Text="{Binding SelectedOrder.from, Mode=TwoWay}" FontSize="Small"/>
            
            <Label Text="Status" FontSize="Default"/>
            <Entry Text="{Binding SelectedOrder.status, Mode=TwoWay}" FontSize="Small"/>
            
            <Button Text="Update Order" Command="{Binding SubmitCommand}" HorizontalOptions="Center"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>