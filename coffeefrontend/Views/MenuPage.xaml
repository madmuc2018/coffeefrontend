<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    Title="Menu"
    xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="using:coffeefrontend"
    x:Class="coffeefrontend.MenuPage">
    <ContentPage.Content>
        <StackLayout>
        <ListView x:Name="listView" x:FieldModifier="public" SeparatorVisibility="None">
           <ListView.ItemsSource>
                <x:Array Type="{x:Type local:MenuPageItem}">
                    <local:MenuPageItem Title="Home" TargetType="{x:Type local:HomePage}" />
                    <local:MenuPageItem Title="Include" TargetType="{x:Type local:NewOrderPage}" />
                </x:Array>
            </ListView.ItemsSource>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="5,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="30"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Source="{Binding IconSource}" />
                            <Label Grid.Column="1" Text="{Binding Title}" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
    </ContentPage.Content>
</ContentPage>
